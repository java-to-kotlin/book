= Parse, Don't Validate

Don't map an on-the-wire format into objects and then validate the correctness of the objects.

Instead, do the validation while you are parsing/mapping.

Objects should never exist in an invalid state.
